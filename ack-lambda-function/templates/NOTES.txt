1. Get the lambda function by running these commands:
kubectl get version --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "ack-lambda-function.name" . }}" -o jsonpath="{.items[0].metadata.name}"